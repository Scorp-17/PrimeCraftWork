try {
  var dict = {
    pl: {
      "nav.about":"O nas","nav.services":"Usługi","nav.why":"Dlaczego my","nav.projects":"Realizacje","nav.contact":"Kontakt","nav.cta":"Napisz do nas",
      "hero.eyebrow":"PrimeCraftWork · Rusztowania w Belgii",
      "hero.title":"Nasze doświadczenie w Twoich projektach",
      "hero.subtitle":"Od lat pracujemy przy montażu i demontażu rusztowań na budowach w Belgii. Teraz chcemy współpracować bezpośrednio z inwestorami i wykonawcami — bez pośredników.",
      "hero.cta":"Napisz do nas","hero.ctaSecondary":"Zobacz usługi",
      "about.kicker":"O nas","about.title":"Wieloletnie doświadczenie, bezpośrednia współpraca",
      "about.p1":"Wspólnie z doświadczonym zespołem monterów rusztowań oferujemy profesjonalne wsparcie dla firm realizujących projekty w Belgii. Nasi pracownicy posiadają wieloletnie doświadczenie w montażu i demontażu rusztowań oraz od lat pracują przy realizacji projektów na terenie Belgii.",
      "about.p2":"Do tej pory realizowaliśmy zlecenia głównie za pośrednictwem innych wykonawców. Obecnie chcemy wykorzystać nasze doświadczenie bezpośrednio we współpracy z inwestorami i wykonawcami oraz budować długoterminowe, stabilne relacje biznesowe.",
      "about.quote":"„Osobiście na co dzień pracuję na budowach razem z zespołem, dzięki czemu mogę bezpośrednio nadzorować jakość wykonywanych prac i osobiście odpowiadać za ich prawidłową realizację.”",
      "services.kicker":"Usługi","services.title":"Co robimy","services.subtitle":"Pełny zakres prac związanych z rusztowaniami na budowach w Belgii.",
      "services.i1.title":"Montaż rusztowań","services.i1.text":"Montaż systemów rusztowań na budowach przemysłowych i komercyjnych w Belgii.",
      "services.i2.title":"Demontaż","services.i2.text":"Sprawny i bezpieczny demontaż konstrukcji po zakończeniu prac na obiekcie.",
      "services.i3.title":"Osobisty nadzór jakości","services.i3.text":"Codziennie pracujemy na budowie razem z zespołem — nadzorujemy jakość i odpowiadamy za wynik osobiście.",
      "services.i4.title":"Transport z HDS","services.i4.text":"Dodatkowo dysponujemy kierowcą z prawem jazdy kat. C+E i doświadczeniem w obsłudze samochodu z HDS.",
      "why.kicker":"Dlaczego my","why.title":"Co nas wyróżnia",
      "why.i1.title":"Doświadczenie w Belgii","why.i1.text":"Od lat pracujemy przy realizacji projektów rusztowaniowych na terenie Belgii i znamy lokalną specyfikę budów.",
      "why.i2.title":"Osobista odpowiedzialność","why.i2.text":"Pracuję na co dzień razem z zespołem na budowie i osobiście odpowiadam za prawidłową realizację prac.",
      "why.i3.title":"Dodatkowo: transport z HDS","why.i3.text":"Dysponujemy kierowcą kat. C+E z doświadczeniem w obsłudze samochodu z żurawiem HDS — dodatkowy atut dla Twojego projektu.",
      "why.i4.title":"Długoterminowa współpraca","why.i4.text":"Zależy nam na stabilnych, wieloletnich relacjach z inwestorami i wykonawcami, nie na jednorazowych zleceniach.",
      "projects.kicker":"Realizacje","projects.title":"Nasza praca na budowie",
      "cta.title":"Szukasz sprawdzonej ekipy do rusztowań w Belgii?","cta.text":"Napisz do nas — porozmawiajmy o Twoim projekcie i możliwościach współpracy.","cta.button":"Napisz do nas",
      "contact.kicker":"Kontakt","contact.title":"Napisz do nas","contact.subtitle":"Odpowiadamy szybko — chętnie omówimy szczegóły współpracy.",
      "contact.emailLabel":"Email","contact.phoneLabel":"Telefon","contact.areaLabel":"Obszar działania","contact.areaValue":"Belgia (oraz okoliczne kraje UE na zapytanie)",
      "contact.form.name":"Imię i nazwisko","contact.form.email":"E-mail","contact.form.phone":"Telefon","contact.form.company":"Firma",
      "contact.form.message":"Wiadomość","contact.form.msgPlaceholder":"Opisz swój obiekt i zakres prac","contact.form.submit":"Wyślij zapytanie",
      "contact.form.consentBefore":"Zapoznałem/-am się z ","contact.form.policyLink":"Polityką prywatności",
      "contact.form.consentAfter":" i wyrażam zgodę na przetwarzanie moich danych osobowych w celu obsługi tego zapytania.",
      "contact.form.consentError":"Zaznacz zgodę na przetwarzanie danych, aby wysłać wiadomość.",
      "footer.copy":"© 2026 PrimeCraftWork. Wszelkie prawa zastrzeżone.",
      "policy.title":"Polityka prywatności",
      "policy.intro":"Niniejsza polityka prywatności opisuje, w jaki sposób PrimeCraftWork przetwarza dane osobowe osób korzystających z formularza kontaktowego na tej stronie internetowej.",
      "policy.h1":"Administrator danych","policy.p1":"Administratorem danych osobowych jest PrimeCraftWork. Kontakt w sprawach związanych z ochroną danych: primecraftwork1@gmail.com.",
      "policy.h2":"Zakres i cel przetwarzania danych","policy.p2":"Za pośrednictwem formularza kontaktowego zbierane są następujące dane: imię i nazwisko, adres e-mail, numer telefonu (opcjonalnie), nazwa firmy (opcjonalnie) oraz treść wiadomości. Dane te są przetwarzane wyłącznie w celu udzielenia odpowiedzi na przesłane zapytanie oraz podjęcia ewentualnej dalszej współpracy.",
      "policy.h3":"Podstawa prawna","policy.p3":"Podstawą prawną przetwarzania danych jest dobrowolnie wyrażona zgoda (art. 6 ust. 1 lit. a RODO), wyrażana poprzez zaznaczenie checkboxa przy wysyłce formularza.",
      "policy.h4":"Odbiorcy danych","policy.p4":"Dane przesłane przez formularz są przechowywane i przetwarzane przy użyciu narzędzi Google (Google Sheets, Gmail) w ramach infrastruktury Google. Dane nie są sprzedawane ani udostępniane innym podmiotom trzecim w celach marketingowych.",
      "policy.h5":"Okres przechowywania","policy.p5":"Dane są przechowywane przez okres niezbędny do obsługi zapytania oraz ewentualnej dalszej korespondencji, nie dłużej jednak niż przez 24 miesiące od ostatniego kontaktu, chyba że dłuższe przechowywanie wynika z obowiązujących przepisów prawa.",
      "policy.h6":"Prawa osoby, której dane dotyczą","policy.p6":"Osobie, której dane dotyczą, przysługuje prawo dostępu do swoich danych, ich sprostowania, usunięcia, ograniczenia przetwarzania, przenoszenia danych, a także prawo do wycofania zgody w dowolnym momencie oraz prawo wniesienia skargi do organu nadzorczego właściwego ds. ochrony danych osobowych.",
      "policy.h7":"Dobrowolność podania danych","policy.p7":"Podanie danych w formularzu jest dobrowolne, jednak niezbędne do udzielenia odpowiedzi na przesłane zapytanie."
    },
    en: {
      "nav.about":"About","nav.services":"Services","nav.why":"Why us","nav.projects":"Projects","nav.contact":"Contact","nav.cta":"Get in touch",
      "hero.eyebrow":"PrimeCraftWork · Scaffolding in Belgium",
      "hero.title":"Our experience in your projects",
      "hero.subtitle":"We've spent years assembling and dismantling scaffolding on job sites across Belgium. Now we want to work directly with investors and contractors — no middlemen.",
      "hero.cta":"Get in touch","hero.ctaSecondary":"See services",
      "about.kicker":"About us","about.title":"Years of experience, direct cooperation",
      "about.p1":"Together with an experienced scaffolding crew, we offer professional support to companies running projects in Belgium. Our team has years of experience in assembling and dismantling scaffolding and has been working on projects in Belgium for years.",
      "about.p2":"Until now we've mainly taken on work through other contractors. We now want to put our experience to direct use working with investors and contractors, and to build long-term, stable business relationships.",
      "about.quote":"“I personally work on site with the team every day, which lets me directly supervise the quality of the work and take personal responsibility for getting it right.”",
      "services.kicker":"Services","services.title":"What we do","services.subtitle":"The full scope of scaffolding work on job sites in Belgium.",
      "services.i1.title":"Scaffolding assembly","services.i1.text":"Assembly of scaffolding systems on industrial and commercial construction sites in Belgium.",
      "services.i2.title":"Dismantling","services.i2.text":"Efficient, safe dismantling of structures once work on site is complete.",
      "services.i3.title":"On-site quality supervision","services.i3.text":"We work on site with the team every day — supervising quality and taking personal responsibility for the result.",
      "services.i4.title":"HDS transport","services.i4.text":"We also have a driver holding a category C+E licence with experience operating a truck-mounted crane (HDS).",
      "why.kicker":"Why us","why.title":"What sets us apart",
      "why.i1.title":"Experience in Belgium","why.i1.text":"We've been working on scaffolding projects in Belgium for years and know the local job-site specifics.",
      "why.i2.title":"Personal accountability","why.i2.text":"I work on site with the team every day and take personal responsibility for getting the work right.",
      "why.i3.title":"Bonus: HDS transport","why.i3.text":"We have a C+E driver experienced with a truck-mounted crane (HDS) — an extra asset for your project.",
      "why.i4.title":"Long-term partnership","why.i4.text":"We're focused on stable, long-term relationships with investors and contractors, not one-off jobs.",
      "projects.kicker":"Projects","projects.title":"Our work on site",
      "cta.title":"Looking for a reliable scaffolding crew in Belgium?","cta.text":"Get in touch — let's talk about your project and how we could work together.","cta.button":"Get in touch",
      "contact.kicker":"Contact","contact.title":"Get in touch","contact.subtitle":"We reply quickly — happy to talk through the details.",
      "contact.emailLabel":"Email","contact.phoneLabel":"Phone","contact.areaLabel":"Coverage area","contact.areaValue":"Belgium (and nearby EU countries on request)",
      "contact.form.name":"Full name","contact.form.email":"Email","contact.form.phone":"Phone","contact.form.company":"Company",
      "contact.form.message":"Message","contact.form.msgPlaceholder":"Describe your site and scope of work","contact.form.submit":"Send inquiry",
      "contact.form.consentBefore":"I have read the ","contact.form.policyLink":"Privacy Policy",
      "contact.form.consentAfter":" and I consent to the processing of my personal data for the purpose of handling this inquiry.",
      "contact.form.consentError":"Please confirm your consent to data processing before sending.",
      "footer.copy":"© 2026 PrimeCraftWork. All rights reserved.",
      "policy.title":"Privacy Policy",
      "policy.intro":"This privacy policy describes how PrimeCraftWork processes the personal data of people using the contact form on this website.",
      "policy.h1":"Data controller","policy.p1":"The controller of personal data is PrimeCraftWork. Contact for data protection matters: primecraftwork1@gmail.com.",
      "policy.h2":"Scope and purpose of processing","policy.p2":"The contact form collects the following data: full name, email address, phone number (optional), company name (optional), and the message content. This data is processed solely to respond to the submitted inquiry and to pursue any further cooperation.",
      "policy.h3":"Legal basis","policy.p3":"The legal basis for processing is freely given consent (Art. 6(1)(a) GDPR), expressed by checking the box when submitting the form.",
      "policy.h4":"Recipients of data","policy.p4":"Data submitted through the form is stored and processed using Google tools (Google Sheets, Gmail) within Google's infrastructure. Data is not sold or shared with third parties for marketing purposes.",
      "policy.h5":"Retention period","policy.p5":"Data is stored for as long as necessary to handle the inquiry and any further correspondence, but no longer than 24 months from the last contact, unless a longer retention period is required by applicable law.",
      "policy.h6":"Rights of the data subject","policy.p6":"The data subject has the right to access their data, rectify it, delete it, restrict its processing, transfer it, withdraw consent at any time, and lodge a complaint with the relevant data protection supervisory authority.",
      "policy.h7":"Voluntary nature of data provision","policy.p7":"Providing data in the form is voluntary, but necessary to respond to the submitted inquiry."
    }
  };

  function applyLang(lang){
    document.documentElement.lang = lang;
    var nodes = document.querySelectorAll('[data-i18n]');
    for(var i=0;i<nodes.length;i++){
      var key = nodes[i].getAttribute('data-i18n');
      if(dict[lang][key] !== undefined) nodes[i].textContent = dict[lang][key];
    }
    var ph = document.querySelectorAll('[data-i18n-placeholder]');
    for(var j=0;j<ph.length;j++){
      var key2 = ph[j].getAttribute('data-i18n-placeholder');
      if(dict[lang][key2] !== undefined) ph[j].setAttribute('placeholder', dict[lang][key2]);
    }
    var lbl = document.getElementById('langLabel');
    if(lbl) lbl.textContent = lang.toUpperCase();
    var btns = document.querySelectorAll('[data-lang]');
    for(var k=0;k<btns.length;k++){
      btns[k].setAttribute('aria-current', btns[k].getAttribute('data-lang') === lang ? 'true' : 'false');
    }
    try{ localStorage.setItem('pcw_lang', lang); }catch(e){}
  }

  var initialLang = 'pl';
  try{
    var saved = localStorage.getItem('pcw_lang');
    if(saved === 'pl' || saved === 'en') initialLang = saved;
  }catch(e){}
  applyLang(initialLang);

  var langButtons = document.querySelectorAll('[data-lang]');
  for(var b=0;b<langButtons.length;b++){
    langButtons[b].addEventListener('click', function(){
      applyLang(this.getAttribute('data-lang'));
      var menu = document.getElementById('langMenu');
      if(menu) menu.classList.remove('open');
    });
  }

  var langBtn = document.getElementById('langBtn');
  var langMenu = document.getElementById('langMenu');
  if(langBtn && langMenu){
    langBtn.addEventListener('click', function(){
      var open = langMenu.classList.toggle('open');
      langBtn.setAttribute('aria-expanded', open);
    });
    document.addEventListener('click', function(e){
      if(!langBtn.contains(e.target) && !langMenu.contains(e.target)) langMenu.classList.remove('open');
    });
  }

  var burgerBtn = document.getElementById('burgerBtn');
  var navLinks = document.getElementById('navLinks');
  if(burgerBtn && navLinks){
    burgerBtn.addEventListener('click', function(){
      var isOpen = navLinks.classList.toggle('open');
      burgerBtn.setAttribute('aria-expanded', isOpen);
    });
    Array.prototype.slice.call(navLinks.querySelectorAll('a')).forEach(function(link){
      link.addEventListener('click', function(){
        navLinks.classList.remove('open');
        burgerBtn.setAttribute('aria-expanded', 'false');
      });
    });
    document.addEventListener('click', function(e){
      if(navLinks.classList.contains('open') && !navLinks.contains(e.target) && e.target !== burgerBtn && !burgerBtn.contains(e.target)){
        navLinks.classList.remove('open');
        burgerBtn.setAttribute('aria-expanded', 'false');
      }
    });
  }

  var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var revealEls = document.querySelectorAll('.reveal');
  if('IntersectionObserver' in window && !reduceMotion){
    var io = new IntersectionObserver(function(entries){
      for(var e=0;e<entries.length;e++){
        if(entries[e].isIntersecting){
          entries[e].target.classList.add('in');
          io.unobserve(entries[e].target);
        }
      }
    }, {threshold:0.12, rootMargin:'0px 0px -40px 0px'});
    for(var r=0;r<revealEls.length;r++){ io.observe(revealEls[r]); }
  } else {
    for(var r2=0;r2<revealEls.length;r2++){ revealEls[r2].classList.add('in'); }
  }

  // lightbox for gallery images
  var galleryImgs = Array.prototype.slice.call(document.querySelectorAll('.g-item img'));
  if(galleryImgs.length){
    var lightbox = document.getElementById('lightbox');
    var lightboxImg = document.getElementById('lightboxImg');
    var lightboxCounter = document.getElementById('lightboxCounter');
    var lightboxClose = document.getElementById('lightboxClose');
    var lightboxPrev = document.getElementById('lightboxPrev');
    var lightboxNext = document.getElementById('lightboxNext');
    var currentIndex = 0;

    function showImage(index){
      currentIndex = (index + galleryImgs.length) % galleryImgs.length;
      var src = galleryImgs[currentIndex].getAttribute('src');
      var alt = galleryImgs[currentIndex].getAttribute('alt') || '';
      lightboxImg.classList.remove('show');
      lightboxImg.src = src;
      lightboxImg.alt = alt;
      lightboxCounter.textContent = (currentIndex + 1) + ' / ' + galleryImgs.length;
      requestAnimationFrame(function(){ lightboxImg.classList.add('show'); });
    }

    function openLightbox(index){
      showImage(index);
      lightbox.classList.add('open');
      document.body.style.overflow = 'hidden';
    }

    function closeLightbox(){
      lightbox.classList.remove('open');
      document.body.style.overflow = '';
    }

    galleryImgs.forEach(function(img, idx){
      img.addEventListener('click', function(){ openLightbox(idx); });
    });

    lightboxClose.addEventListener('click', closeLightbox);
    lightboxPrev.addEventListener('click', function(){ showImage(currentIndex - 1); });
    lightboxNext.addEventListener('click', function(){ showImage(currentIndex + 1); });

    lightbox.addEventListener('click', function(e){
      if(e.target === lightbox){ closeLightbox(); }
    });

    document.addEventListener('keydown', function(e){
      if(!lightbox.classList.contains('open')) return;
      if(e.key === 'Escape') closeLightbox();
      else if(e.key === 'ArrowLeft') showImage(currentIndex - 1);
      else if(e.key === 'ArrowRight') showImage(currentIndex + 1);
    });

    // basic swipe support on touch devices
    var touchStartX = null;
    lightbox.addEventListener('touchstart', function(e){ touchStartX = e.changedTouches[0].clientX; }, {passive:true});
    lightbox.addEventListener('touchend', function(e){
      if(touchStartX === null) return;
      var dx = e.changedTouches[0].clientX - touchStartX;
      if(Math.abs(dx) > 40){ dx < 0 ? showImage(currentIndex + 1) : showImage(currentIndex - 1); }
      touchStartX = null;
    }, {passive:true});
  }

  // ---- contact form: validation, spam protection, Google Apps Script submit ----
  var form = document.getElementById('contactForm');
  if(form){
    var formLoadedAt = Date.now();
    var submitBtn = document.getElementById('cf-submit');
    var statusEl = document.getElementById('formStatus');
    var originalSubmitLabel = submitBtn.textContent;

    var fieldsCfg = [
      { id:'cf-name', errId:'err-name', validate: function(v){
          if(!v.trim()) return { pl:'Podaj imię i nazwisko.', en:'Please enter your name.' };
          if(v.trim().length < 2) return { pl:'Imię jest za krótkie.', en:'Name is too short.' };
          return null;
        } },
      { id:'cf-email', errId:'err-email', validate: function(v){
          var re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
          if(!v.trim()) return { pl:'Podaj adres e-mail.', en:'Please enter your email.' };
          if(!re.test(v.trim())) return { pl:'Nieprawidłowy adres e-mail.', en:'Invalid email address.' };
          return null;
        } },
      { id:'cf-phone', errId:'err-phone', validate: function(v){
          if(!v.trim()) return null; // optional
          var re = /^[+()\d\s-]{6,20}$/;
          if(!re.test(v.trim())) return { pl:'Nieprawidłowy numer telefonu.', en:'Invalid phone number.' };
          return null;
        } },
      { id:'cf-msg', errId:'err-msg', validate: function(v){
          if(!v.trim()) return { pl:'Opisz krótko zakres prac.', en:'Please describe the scope of work.' };
          if(v.trim().length < 10) return { pl:'Wiadomość jest za krótka.', en:'Message is too short.' };
          return null;
        } }
    ];

    function currentLang(){ return document.documentElement.lang === 'en' ? 'en' : 'pl'; }

    function setFieldError(cfg, message){
      var input = document.getElementById(cfg.id);
      var errEl = document.getElementById(cfg.errId);
      var wrap = input.closest('.field');
      if(message){
        if(wrap) wrap.classList.add('has-error');
        if(errEl) errEl.textContent = message[currentLang()];
        return false;
      }
      if(wrap) wrap.classList.remove('has-error');
      if(errEl) errEl.textContent = '';
      return true;
    }

    function validateForm(){
      var allValid = true;
      fieldsCfg.forEach(function(cfg){
        var input = document.getElementById(cfg.id);
        var error = cfg.validate(input.value);
        var ok = setFieldError(cfg, error);
        if(!ok) allValid = false;
      });

      var consentBox = document.getElementById('cf-consent');
      var consentWrap = document.getElementById('consentField');
      var consentErr = document.getElementById('err-consent');
      if(consentBox && !consentBox.checked){
        if(consentWrap) consentWrap.classList.add('has-error');
        if(consentErr) consentErr.textContent = dict[currentLang()]['contact.form.consentError'];
        allValid = false;
      } else {
        if(consentWrap) consentWrap.classList.remove('has-error');
        if(consentErr) consentErr.textContent = '';
      }

      return allValid;
    }

    fieldsCfg.forEach(function(cfg){
      var input = document.getElementById(cfg.id);
      input.addEventListener('blur', function(){
        setFieldError(cfg, cfg.validate(input.value));
      });
      input.addEventListener('input', function(){
        var wrap = input.closest('.field');
        if(wrap && wrap.classList.contains('has-error')){
          setFieldError(cfg, cfg.validate(input.value));
        }
      });
    });

    var consentCheckbox = document.getElementById('cf-consent');
    if(consentCheckbox){
      consentCheckbox.addEventListener('change', function(){
        if(consentCheckbox.checked){
          var consentWrap = document.getElementById('consentField');
          var consentErr = document.getElementById('err-consent');
          if(consentWrap) consentWrap.classList.remove('has-error');
          if(consentErr) consentErr.textContent = '';
        }
      });
    }

    // privacy policy modal
    var policyModal = document.getElementById('policyModal');
    var policyModalClose = document.getElementById('policyModalClose');
    function openPolicyModal(e){
      if(e) e.preventDefault();
      if(policyModal) policyModal.classList.add('open');
    }
    function closePolicyModal(){
      if(policyModal) policyModal.classList.remove('open');
    }
    var policyLinkForm = document.getElementById('policyLinkForm');
    var policyLinkFooter = document.getElementById('policyLinkFooter');
    if(policyLinkForm) policyLinkForm.addEventListener('click', openPolicyModal);
    if(policyLinkFooter) policyLinkFooter.addEventListener('click', openPolicyModal);
    if(policyModalClose) policyModalClose.addEventListener('click', closePolicyModal);
    if(policyModal){
      policyModal.addEventListener('click', function(e){
        if(e.target === policyModal) closePolicyModal();
      });
    }
    document.addEventListener('keydown', function(e){
      if(e.key === 'Escape' && policyModal && policyModal.classList.contains('open')) closePolicyModal();
    });

    function showStatus(kind, textPl, textEn){
      statusEl.className = 'form-status ' + kind;
      statusEl.textContent = currentLang() === 'pl' ? textPl : textEn;
    }

    form.addEventListener('submit', function(e){
      e.preventDefault();
      showStatus('', '', '');

      // honeypot: if filled, it's a bot — pretend success, send nothing
      var honey = document.getElementById('cf-hp');
      if(honey && honey.value){
        showStatus('success', 'Dziękujemy! Wiadomość została wysłana.', 'Thank you! Your message has been sent.');
        form.reset();
        return;
      }

      // time-trap: humans rarely fill and submit a form in under 3 seconds
      if(Date.now() - formLoadedAt < 3000){
        showStatus('error', 'Spróbuj ponownie za chwilę.', 'Please try again in a moment.');
        return;
      }

      // simple client-side rate limit: one submission per 60 seconds per browser
      var lastSubmit = 0;
      try{ lastSubmit = parseInt(localStorage.getItem('pcw_last_submit') || '0', 10); }catch(err){}
      var secondsLeft = Math.ceil((60000 - (Date.now() - lastSubmit)) / 1000);
      if(lastSubmit && secondsLeft > 0){
        showStatus('error',
          'Wiadomość już została wysłana. Spróbuj ponownie za ' + secondsLeft + ' s.',
          'A message was already sent. Please try again in ' + secondsLeft + 's.');
        return;
      }

      if(!validateForm()){
        showStatus('error', 'Popraw zaznaczone pola.', 'Please fix the highlighted fields.');
        return;
      }

      submitBtn.disabled = true;
      submitBtn.textContent = currentLang() === 'pl' ? 'Wysyłanie...' : 'Sending...';
      try{ localStorage.setItem('pcw_last_submit', String(Date.now())); }catch(err){}

      // Submit natively into a hidden iframe instead of using fetch()/XHR.
      // This is plain HTML form behavior — no JS network API involved — so
      // browser extensions that hook fetch/XHR (wallets, ad blockers, etc.)
      // cannot intercept or break it. The iframe's "load" event fires once
      // Google Apps Script has finished processing the request.
      var frame = document.getElementById('hiddenSubmitFrame');
      var settled = false;

      function finishSubmit(){
        if(settled) return;
        settled = true;
        frame.removeEventListener('load', onFrameLoad);
        showStatus('success',
          'Dziękujemy! Wiadomość została wysłana, odpowiemy najszybciej jak to możliwe.',
          'Thank you! Your message has been sent — we will reply as soon as possible.');
        form.reset();
        submitBtn.disabled = false;
        submitBtn.textContent = originalSubmitLabel;
      }

      function onFrameLoad(){ finishSubmit(); }
      frame.addEventListener('load', onFrameLoad);

      // safety net in case the iframe's load event doesn't fire for some reason
      setTimeout(finishSubmit, 6000);

      // no preventDefault beyond this point — let the browser submit the
      // real <form> (action + method + target already set in the HTML)
      HTMLFormElement.prototype.submit.call(form);
    });
  }
} catch(err){
  /* fail silently: CSS-only fallback already shows all content without JS */
  console.error('PrimeCraftWork script error:', err);
}